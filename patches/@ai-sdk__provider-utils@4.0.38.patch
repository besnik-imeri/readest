diff --git a/dist/index.js b/dist/index.js
index 1f739438595d6f4c5d42539bfa195b8fa436426b..84f83049a2101147c8434ece7209a9fcc42965f7 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1638,8 +1638,9 @@ function stringifyRegExpWithFlags(regex, refs) {
     }
     if (flags.m) {
       if (source[i] === "^") {
-        pattern += `(^|(?<=[\r
-]))`;
+        // Avoid lookbehind for older iOS Safari releases (before 16.4).
+        pattern += `(^|[\r
+])`;
         continue;
       } else if (source[i] === "$") {
         pattern += `($|(?=[\r
diff --git a/dist/index.mjs b/dist/index.mjs
index 23b7060646047f724eaeb1a66dcbac6dd824b1f1..d27260e74cd2196c3a45d857c650243be0fc730b 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1543,8 +1543,9 @@ function stringifyRegExpWithFlags(regex, refs) {
     }
     if (flags.m) {
       if (source[i] === "^") {
-        pattern += `(^|(?<=[\r
-]))`;
+        // Avoid lookbehind for older iOS Safari releases (before 16.4).
+        pattern += `(^|[\r
+])`;
         continue;
       } else if (source[i] === "$") {
         pattern += `($|(?=[\r
diff --git a/src/to-json-schema/zod3-to-json-schema/parsers/string.ts b/src/to-json-schema/zod3-to-json-schema/parsers/string.ts
index ef799980b9b6acc81b9fabffd50ed2549e0412f4..524ae10f24b181c3b538efaad1dbc27c1d113ebc 100644
--- a/src/to-json-schema/zod3-to-json-schema/parsers/string.ts
+++ b/src/to-json-schema/zod3-to-json-schema/parsers/string.ts
@@ -388,7 +388,8 @@ function stringifyRegExpWithFlags(regex: RegExp, refs: Refs): string {
 
     if (flags.m) {
       if (source[i] === '^') {
-        pattern += `(^|(?<=[\r\n]))`;
+        // Avoid lookbehind for older iOS Safari releases (before 16.4).
+        pattern += `(^|[\r\n])`;
         continue;
       } else if (source[i] === '$') {
         pattern += `($|(?=[\r\n]))`;
